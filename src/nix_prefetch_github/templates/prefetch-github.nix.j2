let
  pkgs = import <nixpkgs> {};
  src = pkgs.fetchFromGitHub {
    owner = "{{ owner }}";
    repo = "{{ repo }}";
    rev = "{{ rev }}";
    sha256 = "{{ sha256 }}";
  };
in
  import "${src}/overrides.nix" { inherit pkgs; python = pkgs.python3; }
