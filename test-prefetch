#!/usr/bin/env sh

set -e

./clean

nix-shell --command 'PYTHONPATH=${PWD}/src:${PYTHON_PATH} pytest tests/'

nix-build test-exec.nix \
          -A interpreter \
          -o build-package

build-package/bin/nix-prefetch-github \
    seppeljordan \
    pypi2nix
